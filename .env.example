# Comma-separated frontend origins allowed to call FastAPI.
# Example for deployed frontend: https://your-app.vercel.app
FRONTEND_ORIGINS=http://localhost:5173

# Runtime port (cloud platforms inject this automatically).
PORT=8000

# Enable only for local debugging.
UVICORN_RELOAD=false
